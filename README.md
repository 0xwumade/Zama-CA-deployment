# Zama-CA-deployment

🧵 Zama FHEVM x Hardhat Deployment Thread

🚀 Just deployed my first smart contract on Sepolia testnet using Hardhat & FHEVM tools as part of the #ZamaCreatorProgram! Here's the step-by-step breakdown 🧵

1/ 📁 Initialized a new Hardhat project:

```bash
mkdir my-new-project && cd my-new-project
npx hardhat

Selected the "Create a basic sample project" option and installed dependencies.

2/ ⚙️ Installed dotenv to securely manage my keys:

npm install dotenv

Then created a .env file:

INFURA_API_KEY=your_infura_key
PRIVATE_KEY=your_testnet_private_key

3/ 🛠️ Updated hardhat.config.js to load these env vars and connect to Sepolia:

require("dotenv").config();
require("@nomicfoundation/hardhat-toolbox");

module.exports = {
  solidity: "0.8.19",
  networks: {
    sepolia: {
      url: `https://sepolia.infura.io/v3/${process.env.INFURA_API_KEY}`,
      accounts: [process.env.PRIVATE_KEY]
    }
  }
};

4/ 🏗️ Created the default Lock contract in /contracts/Lock.sol (generated by Hardhat):

constructor(uint _unlockTime) payable {
    unlockTime = _unlockTime;
    owner = msg.sender;
}

It requires a timestamp + ETH to be deployed.

5/ 📝 Built my deploy script /scripts/deploy.js:

const hre = require("hardhat");

async function main() {
  const unlockTime = Math.floor(Date.now() / 1000) + 60;
  const lockedAmount = hre.ethers.parseEther("0.0001");

  const Lock = await hre.ethers.getContractFactory("Lock");
  const lock = await Lock.deploy(unlockTime, { value: lockedAmount });

  console.log("✅ Deployed to:", lock.target);
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});

6/ 🧪 Made sure my wallet had Sepolia ETH (used faucet), then deployed!

npx hardhat run scripts/deploy.js --network sepolia

✅ Boom! Contract successfully deployed to:

0xYOUR_DEPLOYED_CONTRACT_ADDRESS

7/ 🧵 Why this matters: This is step 1 toward building encrypted smart contracts using Zama’s FHEVM tech. The contract is live, and now I can build encrypted logic or interact via frontend.

Next up: Add FHE logic or verify it on-chain.

#ZamaCreatorProgram @zama_fhe
